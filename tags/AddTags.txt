#🔭AddTags[position=9][color=#B7A7DB]

Below are some nested tags I created in Zotero. They start with `#` and use `/` as separators:
```js
Zotero.ZoteroGPT.views.messages = [];
JSON.stringify(Zotero.ZoteroStyle.data.views.tagsUI.plainTags.slice(0, 20))
```
---
This is the abstract of a paper:
```js
ZoteroPane.getSelectedItems()[0].getField("abstractNote")
```
---

Based on my existing nested tags, determine which nested tags should be added to this paper. Recommend three, and return a code snippet that can be executed in Zotero. Example:
```js
const tags = ["#tag1/tag2/tag3", "#tag1/tag2/tag3", "#tag1/tag2/tag3"]
item = ZoteroPane.getSelectedItems()[0]
tags.forEach(tag=>{
  item.addTag(tag)
})
await item.saveTx()
```
---
Return code only. Do not add any explanation. Your code is:
